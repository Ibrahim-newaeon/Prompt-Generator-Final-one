---
paths:
  - "src/web/**"
  - "public/**"
---

# Web rules

- Reuse existing API clients, components, tokens, and state patterns.
- Preserve loading, error, empty, accessible keyboard, and responsive behavior.
- Never expose Worker bindings, secrets, or server authorization logic to browser code.
